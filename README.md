# 여행메이트 - 함께 만드는 여행 일정

여행 그룹을 위한 일정 관리, 투표, 공지사항, 메뉴 체크 서비스

## 주요 기능

### 1. 그룹 관리
- **간편한 그룹 생성**: 여행 이름, 기간, 설명을 입력하여 그룹 생성
- **링크 공유 방식**: 별도의 로그인 없이 링크만으로 그룹 참여
- **멤버 관리**: 그룹에 참여한 멤버 목록 확인

### 2. 일정 관리
- **다양한 일정 유형**: 식사, 이동, 숙소, 활동으로 구분
- **상세 정보 입력**: 제목, 설명, 시간, 장소 입력
- **날짜별 그룹화**: 일정을 날짜별로 보기 쉽게 정리
- **알림 설정**: 일정 시작 전 알림 시간 설정

### 3. 공지사항
- **마크다운 지원**: 마크다운 형식으로 풍부한 표현
- **중요 공지 고정**: 상단에 공지 고정 기능
- **실시간 업데이트**: Firestore를 통한 실시간 동기화

### 4. 투표 시스템
- **단일/복수 선택**: 단일 선택 또는 복수 선택 모드 지원
- **실시간 결과**: 투표 결과를 실시간으로 확인
- **진행률 표시**: 각 선택지별 투표 비율 시각화

### 5. 메뉴 사전 체크
- **레스토랑별 메뉴 관리**: 식당 이름과 메뉴 목록 입력
- **멤버별 선택**: 각 멤버가 원하는 메뉴 선택
- **수량 집계**: 메뉴별 선택 인원 자동 집계

## 기술 스택

- **Next.js 15** + TypeScript
- **Material-UI v6** + Emotion
- **Firebase Firestore** - 실시간 데이터베이스
- **Zustand** - 상태 관리
- **Material Design + Flat Design**

## 설치 및 실행

### 1. 패키지 설치

```bash
npm install
```

### 2. Firebase 프로젝트 설정

#### Firebase 콘솔에서 프로젝트 생성
1. [Firebase 콘솔](https://console.firebase.google.com/)에서 새 프로젝트 생성
2. Firestore Database 활성화 (테스트 모드)
3. 프로젝트 설정에서 웹 앱 추가
4. Firebase SDK 구성 정보 복사

#### Firestore 보안 규칙 설정
Firebase 콘솔 > Firestore Database > 규칙 탭에서 다음 규칙 적용:

\`\`\`javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /groups/{groupId} {
      allow read, write: if true;
      match /{subcollection=**} {
        allow read, write: if true;
      }
    }
  }
}
\`\`\`

### 3. 환경 변수 설정

\`.env.local\` 파일 생성:

```bash
cp .env.local.example .env.local
```

Firebase 설정 정보를 \`.env.local\`에 입력:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
```

### 4. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000) 접속

## 빌드 및 배포

```bash
npm run build
npm start
```

### Vercel 배포
1. Vercel 계정에 GitHub 저장소 연결
2. 환경 변수 설정 (Firebase 설정)
3. Deploy 클릭

## 프로젝트 구조

```
travel-group-app/
├── app/                    # Next.js App Router
│   ├── [groupId]/         # 그룹 페이지
│   │   ├── schedule/      # 일정 관리
│   │   ├── announcements/ # 공지사항
│   │   ├── votes/         # 투표
│   │   └── menu/          # 메뉴 체크
├── components/            # React 컴포넌트
├── lib/                   # 유틸리티 및 설정
│   ├── firebase/         # Firebase 설정
│   └── hooks/            # Custom Hooks
├── types/                # TypeScript 타입
└── styles/               # 테마 및 스타일
```

## 사용 방법

1. **그룹 생성**: 홈페이지에서 그룹 이름과 여행 기간 입력
2. **링크 공유**: 생성된 링크를 친구들과 공유
3. **일정 추가**: 일정 탭에서 여행 일정 등록
4. **투표 생성**: 결정이 필요한 사항에 대해 투표 생성
5. **메뉴 선택**: 레스토랑 메뉴를 미리 선택하여 주문 준비

## 라이선스

MIT License

---

**여행메이트**로 더 즐거운 여행을 만들어보세요! 🌏✈️
